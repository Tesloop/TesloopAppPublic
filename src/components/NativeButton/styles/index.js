import {
	Platform,
	StyleSheet
} from 'react-native';

import fonts from 'Tesloop/src/settings/fonts';

let styles = StyleSheet.create({
});

// IOS Platform
if ( Platform.OS === 'ios' ){
// Android Platform
} else {
}

export default styles;
